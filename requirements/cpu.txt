# Common dependencies
-r common.txt

numba == 0.60.0; python_version == '3.9' # v0.61 doesn't support Python 3.9. Required for N-gram speculative decoding
numba == 0.61.2; python_version > '3.9'

# Dependencies for CPUs
packaging>=24.2
setuptools>=77.0.3,<80.0.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1+cpu; platform_machine == "x86_64" # Updated to 2.7.1 for string default support
torch==2.7.1+cpu; platform_system == "Darwin"
torch==2.7.1+cpu; platform_machine == "ppc64le"
torch==2.7.1+cpu; platform_machine == "aarch64" # Updated to 2.7.1 for string default support

# required for the image processor of minicpm-o-2_6, this must be updated alongside torch
# PyTorch 2.7.x pairs with torchaudio 2.7.x
torchaudio; platform_machine == "x86_64"
torchaudio; platform_machine == "aarch64"
torchaudio; platform_machine == "ppc64le"

# required for the image processor of phi3v, internvl, qwen-vl, this must be updated alongside torch
# PyTorch 2.7.x pairs with torchvision 0.20.x
torchvision; platform_machine == "x86_64"
torchvision; platform_machine == "aarch64"
torchvision; platform_machine == "ppc64le"
datasets # for benchmark scripts

# Intel Extension for PyTorch, only for x86_64 CPUs
intel-openmp==2024.2.1; platform_machine == "x86_64"
# intel_extension_for_pytorch==2.6.0; platform_machine == "x86_64" # Disabled for PyTorch 2.4.0 compatibility
# triton==3.2.0; platform_machine == "x86_64" # Disabled for PyTorch 2.4.0 compatibility

# Use this to gather CPU info and optimize based on ARM Neoverse cores
py-cpuinfo; platform_machine == "aarch64"
